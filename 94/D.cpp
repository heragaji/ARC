#include<iostream>
#include<vector>
#include<cmath>

using namespace std;

int main(){
    int Q;
    vector<long long> A,B;
    cin >> Q;
    A.resize(Q);
    B.resize(Q);
    for(int i=0;i<Q;i++){
        cin >> A[i] >> B[i];
    }
    for(int i=0;i<Q;i++){
        long long score = A[i]*B[i];
        double a,b;
        
    return 0;
}